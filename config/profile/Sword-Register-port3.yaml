spring:
  application:
    name: Sword-Register3

server:
  port: 8763

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: prot3
